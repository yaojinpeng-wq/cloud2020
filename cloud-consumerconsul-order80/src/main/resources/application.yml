server:
  port: 80

spring:
  application:
    name: cloud-consumerconsul-order80

  cloud:
    consul:
      port: 8500
      host: localhost
      discovery:
        service-name: ${spring.application.name}